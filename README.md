# SAI8

`SAI8` is an assembler for the Stone Age Instructionset, targeting an 8-bit computer built in Minecraft. It supports program control and Minecraft IoT-style automation via Elbert v2.

# Build

Requires an `ghdl`.

```bash
make
```
